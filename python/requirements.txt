# RAG Edu — Python závislosti
# Instalace: pip install -r python/requirements.txt

# Embeddingy
sentence-transformers>=3.0.0
# Výchozí model: intfloat/multilingual-e5-large (~560MB, stáhne se automaticky)

# Vector store
chromadb>=0.5.0

# LLM a RAG
langchain>=0.3.0
langchain-community>=0.3.0
langchain-chroma>=0.1.0
langchain-huggingface>=0.1.0

# LLM inference přes Ollama (lokální)
ollama>=0.3.0

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Utilities
tqdm>=4.66.0
pydantic>=2.0.0

# Volitelné — překlad (pokud chceš překládat data před embeddingem)
transformers>=4.44.0   # pro OPUS-MT nebo NLLB-200
torch>=2.4.0            # potřeba pro transformers
sentencepiece>=0.2.0    # tokenizer pro MarianMT

# Dev/testy
pytest>=8.0.0
